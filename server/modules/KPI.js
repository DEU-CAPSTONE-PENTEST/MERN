import mongoose from "mongoose"; //bura normalde currency için kullanılıyor, bizim datamız farklı olacağından çok işe yaramıyor

const Schema = mongoose.Schema;
loadType(mongoose);

const daySchema = new Schema({

})
